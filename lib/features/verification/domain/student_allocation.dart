class StudentAllocation {
  const StudentAllocation({required this.studentId, required this.examSessionId, required this.venueId});

  final String studentId;
  final int examSessionId;
  final int venueId;
}
